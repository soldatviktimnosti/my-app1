{
    "name": "my-node-app",
    "version": "1.0.0",
    "scripts": {
        "start": "node app/app.js",
        "dev": "PORT=8080 nodemon app/app.js",
        "test": "jest --passWithNoTests",
        "lint": "eslint app/ test/  --config eslint.config.js",
        "docker:build": "docker build -t my-node-app .",
        "docker:run": "docker run -p 8080:8080 my-node-app"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}